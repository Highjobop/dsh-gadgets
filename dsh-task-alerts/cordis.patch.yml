# dsh-task-alerts bundle patch. Installed through `dsh plugin` (bundle layer) or by
# adding the same row to the profile's own cordis.patch.yml.
- insert:
    - id: dsh-task-alerts
      name: dsh-task-alerts
